# 信号争夺 · 开发日志

> 更新时间：2026-08-06
> 低配电脑可运行的单机人机射击肉鸽小游戏（无需安装依赖）

---

## 一、项目概览

- **运行**：`node server.js` → 浏览器打开 `http://localhost:3000`
- **架构**：**纯前端**（v2）——游戏逻辑在浏览器本地运行（`game.js`），Canvas 渲染与 UI（`app.js`），无需服务器。早期为 Node 服务端 + WebSocket（见下方历史）
- **节奏**：游戏逻辑 `setInterval` 60fps；渲染走 `requestAnimationFrame`
- **设计初衷**（Codex 早期对话）："电脑红方 + 手机蓝方"双人对抗。因实际只有一台电脑，最终以**单人 vs AI** 形式落地；手机/双人功能**未真正实现**（本日志中多次问题由此埋下）

### 代码结构

| 文件 | 职责 |
|---|---|
| `game.js` | 游戏规则、AI、经验/升级逻辑（浏览器端） |
| `app.js` | 输入、渲染、HUD、属性面板与升级界面 |
| `index.html` / `style.css` | 页面与样式 |
| `docs/开发日志.md` | 本日志 |

> 早期版本使用 `server.js`（Node 服务端 + WebSocket），后改为纯前端，`server.js` 已删除。

---

## 二、迭代记录（按 git 提交）

### 2.1 初始提交（`7b0fa33`）

- 单人 vs AI：90 秒内抢能量核心，每 3 枚触发三选一强化
- 已有核心玩法：波次、核心、强化、击败 AI
- 已知隐患：单玩家实体 + `game.controller` 由"最后 join 者"担任

### 2.2 第 1 轮 · 输入处理优化

**目标**：解决按键卡住/失灵。

- 键位改用物理键码 `e.code`（不受键盘布局影响），`ShiftLeft/ShiftRight` 均支持冲刺
- 屏蔽中文输入法（IME）合成事件，避免 keyup 被吞导致按键卡住
- 失焦、切标签页、IME 启动时自动清空按键状态
- 按住键自动重复只处理一次

### 2.3 第 2 轮 · 移动逻辑修复（`bb6919b` 的前半部分）

**目标**：修复"无法移动"的隐性逻辑问题。

- 死亡重生：原逻辑死后原地冻结 2.6s 再瞬移回地图中心，且 bot 接触伤害不检查 `respawn`，被围住会**反复触发死亡、永久卡死**
  - 改为：原地重生 + 1.5 秒无敌、不再瞬移；接触伤害与子弹都尊重 `respawn`/`invuln`
- 修复竖直瞄准 bug：服务端 `Number(aimX) || 1` 会把合法的 `0` 改写为 `1`，导致无法垂直瞄准
- 服务端 tick 由 30fps 提到 60fps（AI 开火概率按 `dt` 重平衡以保持原射击频率）

### 2.4 第 3 轮 · 客户端预测与平滑渲染（`bb6919b`）

**目标**：消除"移动不跟手 / 顿挫"。

- 客户端玩家位置预测：用当前输入即时计算速度，从服务端权威位置外推，按键响应接近零延迟；平滑追赶避免瞬移/回弹，静止零漂移
- 服务端下发玩家当前 `speed`（含升级后），客户端据此精确预测
- bot 平滑渲染（跨 tick 收敛）
- 验证数据（Node 模拟 60fps）：静止收敛 0.00px、停止响应 4 帧、反向 2 帧、预测误差封顶 9.7px

### 2.5 第 4 轮 · 控制器冲突修复（`c009bc7`）

**目标**：修复"只能小范围移动 + 子弹发射不了"。

- **根因**：游戏本意双人，但实现只有单一 `game.player`。任何客户端 join 或发输入都会成为 `game.controller`。手机（或残留标签页）一连上就抢走控制权 → 电脑 WASD/空格输入被忽略 → 角色停在原地（小范围抖动为客户端预测在冻结位置附近的抖动），子弹发不出
- 改动：只有发出**非零移动/射击输入**的客户端才成为控制器；观战客户端不抢占

### 2.6 第 5 轮 · 移除观战逻辑（`274e553`）

**目标**：修复"我电脑进去也显示观战"。

- **根因**：上一轮把控制器改为"按下非零键才算"，打开页面未按键时 `controller` 为空，被误判为观战
- 改动：打开页面（join）即成为控制端；移除 `you`/观战 UI；README 改为单人玩法提示

### 2.7 第 6 轮 · 输入法 keyCode 229 修复（`813bbc6`）

**目标**：修复"彻底无法移动和攻击"。

- **根因**：Windows + Chrome + 中文输入法环境下，W/A/S/D 的 `keydown` 会上报 `keyCode=229`（VK_PROCESS），`e.key` 可能为 `"Process"`。此前用 `keyCode===229` 屏蔽输入法按键，把正常 WASD 也误杀
- 改动：改用物理键 `e.code` + 仅 `e.isComposing` 屏蔽真正的输入法合成，另加 `e.key` 兜底映射
- 验证：7 种按键事件形态全部映射正确（含"输入法活动但 `e.key=Process`"场景）

### 2.8 第 7 轮 · 诊断进行中（`b1edaa5` → `207e662`）

**目标**：定位"玩家仍动不了 + 再来一局无反应"。

- 已确认：浏览器跑最新代码、`ws=1` 连接正常、`you=true` 是控制端、客户端确实发出 `输入=(1,0)`
- 已确认：服务器端能正常响应输入（工具客户端实测移动 291px、射击出弹）
- 已加诊断：页面左上角临时调试面板（版本号 / ws / you / 输入 / 收到 / 应用 / 玩家位置）+ 服务器 `[net]` 日志

### 2.9 第 8 轮 · 根因闭环（`20d4554`）

**目标**：自主闭环"玩家动不了 + 再来一局无反应"（不再依赖用户手动测数据）。

- 搭建**无头浏览器测试**（puppeteer-core + 系统 Chrome 无头模式），真实复现：keydown 正常、客户端发 x=1，但服务器收到 x=0
- 加服务器逐帧日志确认：收到的 input 全是默认零输入，`start` 正常而 `input` 丢失
- **定位根因**：WebSocket 帧解析器不支持 16 位扩展长度帧（载荷 ≥126 字节）；浏览器输入消息带长小数 aimX/aimY 超长触发扩展帧，解析错位 → 帧流脱同步 → input 全丢
- **修复**：`size===126` 时用 `readUInt16BE` 读取真实 16 位长度
- 无头浏览器实测：按住 D 移动 214px、空格出弹、无 JS 错误
- 清理全部诊断日志/面板，保留 `window.__state` 便于后续调试

### 2.10 功能：属性面板与 XP 系统（`18b2dae`）

- **属性面板**：左下角实时显示 等级/生命/伤害/射速/弹速/移速/拾取/减伤，达到上限标"满"
- **XP 进度条**：右上角显示当前等级所需经验进度
- **经验曲线**：`xpToNext(level) = 3 + ⌊(level-1)×1.5⌋`，升级所需经验随等级递增（3、4、6、7、9、10、12…）
- **`addXp` 统一处理**经验累加与升级触发，支持一次多级
- **属性上限**：射速≥0.35、移速≤410、伤害≤80、生命≤400、拾取≤200、弹速≤1500、减伤≤48%（留足余地，避免过早封顶）
- 已满级的强化不再出现在三选一池中

### 2.11 架构：转纯前端 + GitHub Pages 部署（`待提交`）

- **动机**：GitHub Pages 只能托管静态文件，无法运行 Node 服务端，因此把游戏逻辑从 `server.js` 整体移植到浏览器端 `game.js`
- **改动**：
  - 新增 `game.js`：游戏规则、AI、经验/升级逻辑全部浏览器本地运行（tick 60fps），输入由 `game.input` 直接提供
  - `app.js` 改为无服务器：移除 WebSocket/消息/客户端预测，直接读写本地 `game`，开始/升级按钮直调函数
  - 删除 `server.js`（不再需要 Node）
  - 文案修正：升级描述由"抢到 3 枚核心"改为经验驱动
  - `index.html` 现在可直接 `file://` 打开，或部署到 GitHub Pages
- **验证**：无头浏览器 `file://` 打开实测——开局/移动/射击/升级面板/选择强化/属性面板/重生 全部正常，无 JS 错误

### 2.12 玩法改版：无尽生存 / 生命系统 / 手机双摇杆

- **正计时 + 无尽生存**：去掉波次限时倒计时，改为正计时存活时间；清完一波自动进下一波，游戏不再超时结束
- **3 条命**：血条耗尽→减一条命→原地满血重生（无敌 1.5s）；最后一条命用完即本局结束
- **新奖励**：升级池新增"多一条命"（上限 5 条）；"增加生命上限"保留
- **手机双摇杆**：左半屏摇杆移动、右半屏摇杆瞄准、自动开火；`pointer: coarse` 检测触屏；HUD 隐藏属性面板、显示"命×N"
- **验证**：无头浏览器实测——正计时递增、减命重生、命用完结束、多命奖励、无尽不超时；移动视口下双摇杆移动/瞄准/自动开火/松开归零全部正常，无 JS 错误

### 2.13 暂停页 / 游戏结束页 / 减命 Bug 修复

- **暂停页**：电脑 `ESC` 触发，手机右上角 ⏸ 按钮；暂停时完全冻结（时间/敌人/子弹不动）；含"继续 / 重新开始"；**手机端**把属性面板放进暂停页（`buildAttr` 复用）
- **游戏结束页**：最后一条命用完即结束，展示统计（存活时间 / 波次 / 能量），可重新开始
- **修复减命 Bug**：被**子弹**杀死时，子弹逻辑残留旧的 `respawn=2.6` 设置，导致减命判定被跳过（命不减）；被接触伤害杀死则正常减。修复：移除子弹里的 `respawn=2.6`，统一由死亡判定处理
- **验证**：无头浏览器实测——ESC/按钮暂停、暂停冻结、继续、暂停菜单重开、子弹杀死减命（3→2）、命用完结束面板+统计、结束重开、手机暂停按钮+暂停页属性面板 全部正常，无 JS 错误

### 2.14 道具系统 / Boss 种类 / 敌人成长

- **敌人成长**：非 Boss 小怪（侦察/射手/重装）血量按 `1+(wave-1)×0.12`、伤害按 `1+(wave-1)×0.1` 随波数递增，越后期越难
- **三种首领**：每 3 波出现，按 `BOSS_TYPES` 轮换——`boss` 普通首领（三连发）、`fireboss` 喷火首领（每 2.6 秒朝玩家扇形喷射 5 枚火焰弹，火焰弹带 `fire` 标记、判定更大）、`tankboss` 堡垒首领（血量 820 / 移速 46 / 近战 26 伤害）
- **掉落道具**：小怪 15% 概率掉落、首领必掉；道具持续 12 秒。四种类型：
  - `medkit` 血包：回复 45 生命
  - `fury` 怒火 / `overclock` 超频 / `snipe` 射程增幅：8 秒临时增益，**可突破属性上限**（伤害 ×1.5 / 攻速 ×1.6 / 子弹射程 ×1.8）
- **实现要点**：`actor.buffs` 存增益剩余时间，`stepActor` 发射时按增益计算伤害/射速/子弹 `life`，tick 每秒衰减；`game.items` 数组 + `spawnItem`/`applyItem`；HUD 右上角 buff 倒计时徽章
- **验证**：无头浏览器实测——小怪血量 45→67（wave5）、Boss 轮换 火→肉→普通、怒火弹伤害 27（18×1.5）、射程弹 1.476s（0.82×1.8）、喷火 boss 出 5 枚火焰弹、boss 击杀必掉 + 拾取生效 + 增益随时间衰减 全部正常，无 JS 错误

### 2.15 Boss 血条置顶 / Boss 战小怪

- **Boss 血条置顶**：Boss 存活期间，顶部中央显示 Boss 血条（名称 + 血条 + 血量数字），**代替计时器位置**（计时器隐藏但继续走）；Boss 死亡后血条消失、计时器恢复显示
- **Boss 战小怪**：首领波时，小怪改为聚集在 Boss **周围一定区域**（半径约 130–280px）内刷新，不再是全图随机；数量 = `max(2, floor(wave/3))`
- **实现要点**：`makeEnemy` 新增 `near` 参数（以某点为中心聚集）；`frame()` 每帧检测 `state.bots` 中是否存在 `boss`，切换血条/计时器显隐
- **验证**：无头浏览器实测——Boss 存在时血条显示「喷火首领 420/420」、计时器隐藏但仍在走、Boss 死亡后血条消失计时恢复、Boss 波小怪全部刷在 Boss 周围 400px 内 全部正常，无 JS 错误

### 2.16 UI 重构：经验系统 / HUD 布局

- **经验数值调整**：拾取 1 个能量核心 = **+10 经验**；击杀经验按怪种区分且整体下降——侦察 `+2`、射手 `+3`、重装 `+4`、首领 `+15`
- **HUD 布局重构**：
  - **左上角**：等级（`LV 13`）+ 血条 + 经验条 + 红心命（`♥×N` 图形，代替文字「命×3」）
  - **右上角**：波次 + 暂停键
  - **左下角**：属性面板（保留 能量/伤害/射速/弹速/移速/拾取/减伤，去掉左上已显示的 等级/生命/命），背景透明度提高
  - 中央计时器保留；Boss 战时仍由 Boss 血条暂时替代计时器
- **实现要点**：`index.html` 顶部 HUD 重构为 `hud-left`（等级/血条/经验条/红心）+ 中央计时 + 右上波次；移除旧 `#score`、`#level`、`.xp` 元素；`buildAttr` 精简属性项
- **验证**：无头浏览器实测——核心拾取 +10、击杀经验分怪种（侦察 2/射手 3/重装 4）、`LV 1`/血条 100%/经验条 0%/红心 ♥♥♥、波次显示、左下属性面板去重后正常 全部通过，无 JS 错误

### 2.17 HUD 布局微调：血条数字 / 经验条 / 右上角 / 右下角

- **左上角 HUD**：第一排「等级 + 红心命数」；第二排血条、第三排经验条
  - 血量数字在血条**中心上方**（`position:absolute; top:-15px`），格式「当前/上限」（如 `60/100`），**红色**
  - 经验数字同理，格式「当前经验/上限」（如 `2/3`），**黄色**；经验条样式与血条一致（同宽同高、圆角填充）
- **右上角**：暂停键改为**方形**（42×42，圆角 8px）置于最右上角；**波次 + 当前敌人数**放在暂停键左侧（`.top-right`）
- **右下角**：游戏名「信号争夺」置于右下角，**除文字外完全透明**
- **移除顶部标题栏**（`header` 及其 `#status`）；计时器保留在顶部中央
- **实现要点**：`index.html` 重构 `.top` 结构（`hud-left` + 计时器 + `.top-right`）；`app.js` 移除 `status` 引用、新增 `hudHpNum`/`hudXpNum`/`enemyCount` 更新；`style.css` 删 `header` 样式、新增 `.top-right`/`.game-title`
- **验证**：无头浏览器实测——`LV 1`/`60/100`(红)/`2/3`(黄)、红心 ♥♥♥、波次+敌人数位于方形暂停键左侧、计时器中央、游戏名右下角透明、旧 header 移除 全部正常，无 JS 错误
- **微调**：血条/经验条数字改为**条内居中**（`top:50%` + `translate(-50%,-50%)` + `line-height:1`，z-index 高于填充条，配深色描边保证可读）；移除底部 footer 说明文字
- **微调**：血条/经验条与 Boss 血量数字改**白色 + 深色描边**提高对比度
- **调整**：升级所需经验**乘 5**——`xpToNext = (3+⌊(level-1)×1.5⌋)×5`，新曲线 15/20/30/35/45/50/60…（经验获取量不变：核心 +10、击杀 2/3/4/15）
- **调整**：Boss 血量提高并随首领出场次数成长——普通 360→**500**、喷火 420→**580**、堡垒 820→**1100**；第 2 次出场起每次血量 ×1.3（堡垒：波3 1100 → 波6 1430 → 波9 1760 → 波12 2090）

---

## 三、Bug 记录与状态表

| # | 症状 | 根因 | 状态 |
|---|---|---|---|
| 1 | 按键卡住/失灵 | 键位用 `e.key`、IME 吞 keyup、失焦不清键 | ✅ 已修复 |
| 2 | 死亡后卡死、瞬移回中心 | respawn 期间接触伤害未屏蔽、死亡反复触发 | ✅ 已修复 |
| 3 | 无法垂直瞄准 | `Number(aimX) \|\| 1` 把 0 改写成 1 | ✅ 已修复 |
| 4 | 移动顿挫不跟手 | 渲染直接用服务端位置、tick 30fps | ✅ 已修复（60fps + 客户端预测） |
| 5 | 只能小范围移动 + 发不出子弹 | 第二客户端（手机/旧标签页）抢占控制器 | ✅ 已修复 |
| 6 | 电脑显示"观战" | 打开页面未按键时 controller 为空 | ✅ 已修复 |
| 7 | 彻底无法移动/攻击 | `keyCode===229` 误杀 WASD（中文输入法环境） | ✅ 已修复 |
| 8 | **玩家仍动不了 + 再来一局无反应** | WebSocket 帧解析器不支持 16 位扩展长度帧 | ✅ 已修复 |

---

## 四、核心问题：根因与修复（已闭环）

**症状**：按住 D 时面板 `输入=(1,0)` 但 `应用=(0,0)`，玩家不动；点"再来一局"无反应。

**根因**：服务器自实现的 WebSocket 帧解析器**只支持 125 字节以内的短帧**。浏览器发送的输入消息带上鼠标算出的长小数（`aimX:0.7071067811865476`），JSON 超过 125 字节，浏览器按 RFC 6455 改用**"16 位长度"扩展帧**发送；而解析器用 `byte1 & 127` 当长度、**忽略了真正的 16 位长度字段** → 解析错位、帧流脱同步，**后续所有 input 帧全部丢失**。`start` 等短消息正常，所以游戏能开始但动不了/射不了。

**为何此前测不出来**：手工测试客户端一直发 `aimX:1`（短消息 <125 字节）没触发；真实浏览器发长小数才触发。

**修复**：`size===126` 时用 `readUInt16BE(2)` 读取真实长度并正确跳过头部。

**验证**：无头浏览器（Chrome）实测——按住 D 玩家移动 214px、空格出弹、无 JS 错误。

---

## 五、诊断手段沉淀

1. **服务端集成测试**：用自实现 WebSocket 客户端（手工掩码帧）连 3000/测试端口，验证 join→start→移动→射击 全链路
2. **客户端管线测试**：Node 加载真实 `app.js` + 桩 DOM/Canvas/WebSocket，逐帧验证按键→input→渲染无异常
3. **渲染数学测试**：模拟 60fps 服务端 + 60fps 客户端（含 1 tick 延迟），验证预测/平滑/收敛
4. **线上探测**：被动连接 3000 观察真实状态（phase、玩家位置、版本字段）
5. **页面调试面板**：左上角实时显示版本/连接/输入/收到/应用/玩家位置，供用户直接读数（定位后已移除）
6. **无头浏览器测试（关键）**：puppeteer-core + 系统 Chrome 无头模式，真实加载 app.js 连服务器，模拟按键，读游戏状态验证移动/射击。**复现并闭环了核心 bug**（工具在 `%TEMP%\ss_diag`）

---

## 六、git 历史

```
18b2dae feat: 属性面板与 XP 系统
20d4554 修复：WebSocket 帧解析器不支持 16 位扩展长度帧（核心 bug，已闭环）
84a3ccd 文档：新增开发日志
207e662 诊断：面板增加 收到=字段
dd4a877 诊断：服务器终端打印 join/input/start
ff5238b 诊断：面板增加 you 与应用回显
b1edaa5 诊断：加临时调试面板
813bbc6 修复：中文输入法 keyCode 229 误杀 WASD
274e553 移除观战逻辑：电脑打开页面即控制端
c009bc7 修复：第二客户端抢占控制权
bb6919b 优化：移动响应与平滑渲染
7b0fa33 初始提交：信号争夺游戏
```
